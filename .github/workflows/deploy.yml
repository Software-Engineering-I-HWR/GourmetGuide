name: deploy to server
on: workflow_dispatch

jobs:
  check-admin:
    runs-on: ubuntu-latest
    steps:
      - id: check-auth
        run: |
          ADMIN_USERS="Canoobi blnpurple"
          if [[ ! " $ADMIN_USERS " =~ " ${{ github.actor }} " ]]; then
            echo "You are not authorized to run this workflow."
            exit 1
          fi

  pull-repo:
    needs: check-admin
    runs-on: ubuntu-latest
    steps:
      - name: Pull from Git
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.NAS_HOST }}
          username: ${{ secrets.NAS_USER }}
          password: ${{ secrets.NAS_SSH_KEY }}
          script: |
            echo "${{ secrets.NAS_SSH_KEY }}" | sudo -S /usr/local/bin/docker start GourmetGuide-Pull-From-Git

  restart-containers:
    needs: pull-repo
    runs-on: ubuntu-latest
    steps:
      - name: Restart containers
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.NAS_HOST }}
          username: ${{ secrets.NAS_USER }}
          key: ${{ secrets.NAS_SSH_KEY }}
          script: |
            docker restart GourmetGuide-DB-API
            docker restart GourmetGuide-DB-API-Login
            docker restart GourmetGuide-Login-API
            docker restart GourmetGuide-PDF-API
            docker restart GourmetGuide-ImageUpload-API
            docker restart GourmetGuide-Global-API
            docker restart GourmetGuide-UI